# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.env
.env.*
!.env.example
coverage/
tsconfig.tsbuildinfo
src/**/*.js
dev-dist/

# Local agent state — worktrees here are embedded git repos, must not be committed.
.claude/
# Claude Code's per-project instructions — local-only, per the project convention.
CLAUDE.md

# Design source files — large binary PSDs / AIs. Live in the design tool /
# shared drive, not the code repo. Specs in docs/ are the canonical text.
App Design 設計/
*.psd
*.ai
